server:
  port: 7708

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userservice_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
